{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "packages/db/Dockerfile.migrate"
  },
  "deploy": {
    "restartPolicyType": "NEVER"
  }
}
